// Configuração da API
// Este arquivo define a URL base do servidor backend.
// Como o site pode ser aberto pelo Live Server (porta 5500) ou diretamente
// pelo servidor Node.js (porta 3000), usamos sempre a URL absoluta do backend.
const API_URL = 'http://localhost:3000';
